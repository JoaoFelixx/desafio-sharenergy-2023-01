import React from 'react';
import { ClientsList, BackButton, } from "components";

export const Clients = () => {

  return (
    <React.Fragment>
      <BackButton />
      <ClientsList />
    </React.Fragment>
  )
}